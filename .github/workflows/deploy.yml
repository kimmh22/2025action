name: Github Action 실행해보기
on:
  push:
    branches:
      - main

jobs:
  My-Deploy-Job:
    runs-on: ubuntu-latest

    steps:
      - name: Hello World 찍기
        run: echo "Hello World"

      - name: 여러문장
        run: |
          echo "good"
          echo "bye"
      - name: 깃허브변수
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REPOSITORY
